{
  "name": "yago",
  "version": "0.0.1",
  "scripts": {
    "lint": "tslint **/*.ts",
    "test": "mocha test/",
    "start-redis": "docker run -d -p 6060:6379 redis"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.0.2"
  },
  "dependencies": {
    "node-uuid": "^1.4.7",
    "redis": "^2.6.2"
  }
}
